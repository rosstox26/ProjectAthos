<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rosstox26.athos.projectathos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".SearchHomeActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".CenterActivity" />
        <activity android:name=".ProviderRegisterActivity" />
        <activity android:name=".SearchResultActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".ReadReviewActivity"></activity>
    </application>

</manifest>